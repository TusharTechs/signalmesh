# Local NATS development config.
# For hackathon usage, the docker-compose command flags are enough.
# This file is kept for future explicit server configuration.
port: 4222
http_port: 8222

jetstream {
  store_dir: /data/jetstream
}
